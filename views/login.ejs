<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet"/>
    <title>Login - Drive</title>
  </head>
  <body class="bg-gradient-to-br from-slate-900 via-blue-900 to-emerald-900 min-h-screen flex items-center justify-center">
    <div class="fixed inset-0 bg-slate-900 -z-10"></div>
    <div class="w-full  max-w-md">
      <div class="text-center mb-8">
        <i class="ri-cloud-line text-4xl text-emerald-400"></i>
        <h1 class="text-3xl font-bold text-white mt-4">Welcome to Drive</h1>
        <p class="text-gray-300 mt-2">Sign in to access your files</p>
      </div>

      <div class="bg-slate-800/90 backdrop-blur-sm rounded-xl shadow-lg p-8">
        <form action="/user/login" method="post">
          <div class="mb-6">
            <label for="username" class="block text-sm font-medium text-gray-300 mb-2">Username</label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                <i class="ri-user-line text-gray-400"></i>
              </div>
              <input type="text" id="username" name="username" required
                class="block w-full pl-10 pr-3 py-2 border border-emerald-400/30 rounded-lg bg-slate-900/50 text-white placeholder-gray-400 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
                placeholder="Enter your username" />
            </div>
          </div>

          <div class="mb-6">
            <label for="password" class="block text-sm font-medium text-gray-300 mb-2">Password</label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                <i class="ri-lock-line text-gray-400"></i>
              </div>
              <input type="password" id="password" name="password" required
                class="block w-full pl-10 pr-3 py-2 border border-emerald-400/30 rounded-lg bg-slate-900/50 text-white placeholder-gray-400 focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
                placeholder="Enter your password" />
            </div>
          </div>

          <button type="submit"
            class="w-full bg-emerald-500 hover:bg-emerald-600 text-white font-medium py-2 px-4 rounded-lg transition-colors duration-200 flex items-center justify-center">
            <i class="ri-login-box-line mr-2"></i>
            Sign In
          </button>

          <div class="mt-6 text-center">
            <a href="/user/register" class="text-sm text-emerald-400 hover:text-emerald-300">
              Don't have an account? Create one
            </a>
          </div>
        </form>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
  </body>
</html>